import { EnterFocusDirective } from './enter-focus.directive';

describe('EnterFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new EnterFocusDirective();
    expect(directive).toBeTruthy();
  });
});
